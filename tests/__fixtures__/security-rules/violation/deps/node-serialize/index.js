// Fixture stub — analyzed code is parsed as data, so this must never run.
module.exports = {
  unserialize() {
    throw new Error("fixture stub executed — the analyzer must never run target code");
  },
};
