// The corpus' ONE default-exported module: it keeps the named-binding
// convention dominant-but-not-unanimous (12/13), and it is the file the
// violation diff modifies to trip `conformance/module-shape`.
const oddThing = 0;

export default oddThing;
