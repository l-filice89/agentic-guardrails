// The UNCHANGED importer that makes odd-thing.ts default-bound in the graph.
// Unchanged is the point: evidence about a judged file must pre-date the diff.
import oddThing from "./odd-thing.js";

export const consumedOddThing = oddThing;
