// Fixture STUB of the real package's surface — copied to the temp repo's
// node_modules so the fixture import resolves (no unresolved-bare-specifier
// degradation). Types only for the compiler; never executed.
export function unserialize(input: string): unknown;
