// MODIFIED by the diff (its path is already in the corpus, so naming and
// placement do NOT judge it) — but its importers, which are not in the diff,
// still bind the minority `default` form.
const oddThing = 1;

export default oddThing;
