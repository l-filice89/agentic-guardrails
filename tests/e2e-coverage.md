# E2E Coverage Map

Per-story map of UI-facing acceptance criteria to end-to-end coverage.

| Story | UI-facing ACs | E2E coverage |
|---|---|---|
| 1.2 Canonical contracts package | None — library package only; first UI surface is the CLI in Story 1.4 | n/a |
| 1.3 TypeScript LanguageAdapter and import graph | None — library layer only; first UI surface is the CLI in Story 1.4 | n/a |
| 1.4 Walking skeleton — first end-to-end review | `guardrails review` CLI flow: circular-import findings in the stdout summary + exit 1; clean tree / no-violation change exit 0; non-repo preflight failure exit 2 (stderr, no artifact); artifact + embedded manifest persisted and schema-valid; byte-identical artifact across identical runs | `tests/integration/walking-skeleton.e2e.test.ts` (spawns the built `packages/cli/dist/index.js` against temp git repos) |
| 1.5 SPIKE-3 import-graph cost at scale | None — measurement spike, no UI flow; deliverable is `docs/spikes/SPIKE-3-import-graph-cost.md` plus the committed harness (`scripts/spike-3-benchmark.mjs`, run manually) | n/a |
| 1.6 Config plane | `_agentic-guardrails/config.yaml` drives `guardrails review`: invalid enum/typo key/malformed YAML → exit 2 with the offending path or line/column on stderr (no stack trace); advisory axiom with error findings exits 0 with findings still printed + persisted (bypass hazard); `maxFindings` threshold boundary (exactly N vs N+1); `off` axiom skipped and declared in the manifest's `axiomsOff`; deviation lines + "using defaults (no config file)" on stderr; `config.schema.json` generated beside the YAML; config content changes the runId | `tests/integration/config-plane.e2e.test.ts` |
| 1.7 Pipeline hardening + deterministic cache | Warm re-run cache-hits (declared in `manifest.cache`, analyzers skipped) with a byte-identical artifact modulo the `manifest.cache` counters (documented carve-out); corrupted cache entry → correct output + degradation note + entry overwritten, exit 2; report header lists degraded work (name + reason) above the findings block; six-phase assembly declared in `manifest.phases` (2/3 empty-membership with reason); planted schema-valid cache entry (cache poisoning) rejected by the HMAC check — recomputed, declared invalid, never served | `tests/integration/pipeline-hardening.e2e.test.ts` |
| 1.8 `init` + structural corpus seed | `guardrails init --no-input` bootstraps `_agentic-guardrails/` (config.yaml from the 1.6 defaults, empty-but-valid conventions.yaml + corpus-map.yaml, `.gitattributes` with `history/*.jsonl merge=union`, seeded `.gitignore`) and writes the file-level structural seed to `.cache/corpus/`, exit 0 with a per-file created/kept summary; non-TTY stdin skips the questionnaire (no prompt blocks); re-init never clobbers a human-edited file (byte-identical, reported "kept") and regenerates the seed byte-identically; non-repo exits 2 naming the git requirement with no partial writes; missing tsconfig → seed skipped with a declared reason; review after init carries real sha256 ledger/corpus hashes with the sentinel degradations gone (whole-run wall clock asserted under the ≤5s NFR-3 preflight ceiling); removed wiring line → stderr warning naming the consequence, exit unchanged; uninitialized review behavior unchanged (sentinels, no warning) | `tests/integration/init.e2e.test.ts` |
| 1.9 Axiom #1 structural rule set | `guardrails review` over the violation fixture (cycle + unresolved import + direction breach + unassigned file) matches `expected-findings.json` byte-for-byte, twice (real import-line anchors, per-rule severities); clean boundaries fixture yields zero findings, exit 0; axiom 1 `off` skips the analyzer and declares it in `axiomsOff`; `advisory` persists the direction finding with a passing gate, exit 0; unresolved-import degradation dominance exits 2 with the finding still the actionable surface | `tests/integration/structural-rules.e2e.test.ts` |
| 1.10 Axiom #3 cleanliness rule set | `guardrails review` over the violation fixture (unreachable code + unused export + structural duplicate + complexity 16) matches `expected-findings.json` byte-for-byte, twice (all four ruleIds, real anchors, per-rule severities, exit 1 with zero degradations — an honest gate verdict); clean fixture yields zero findings, exit 0, and a warm run cache-HITS byte-identically over the 1.10 `names`-carrying graph payload; a graph-payload roundtrip run (findings entries deleted, graph entries kept) rebuilds findings from the deserialized cached graph byte-identically (1 graph hit, one findings miss per registered axiom — 4 since 1.12); axiom 3 `off` skips the analyzer and declares it in `axiomsOff`; `advisory` persists the unreachable-code error finding with a passing gate, exit 0 | `tests/integration/cleanliness-rules.e2e.test.ts` |
| 1.11 Axiom #4 NFR rule set (structural tier) | `guardrails review` over the violation fixture (unbounded `Promise.all(urls.map(...))` + `readFileSync` in an async function + `fetch` with a signal-less options literal) matches `expected-findings.json` byte-for-byte, twice (all three ruleIds, real anchors, axiom "4", all WARNINGS) — and, all findings being warnings by design, the run EXITS 0 with the findings persisted (asserted explicitly, zero degradations); clean fixture (fixed-arity `Promise.all`, sync fs in a sync function, `fetch` with `signal`) yields zero findings, exit 0, and a warm run cache-HITS byte-identically; axiom 4 `off` skips the analyzer and declares it in `axiomsOff`; `advisory` persists the missing-abort-signal finding with a passing gate, exit 0 | `tests/integration/nfr-rules.e2e.test.ts` |
| 1.12 Axiom #5 security rule set (regex/AST tier) | `guardrails review` over the violation fixture (fake `AKIA…` key + fake `github_pat_…` PAT in COMMENTS + secret-named assignment + template-SQL `query` + `eval` + string `setTimeout` + node-serialize `unserialize`) matches `expected-findings.json` byte-for-byte, twice (all four ruleIds, both source tiers `regex`+`ast`, error/warning per doctrine) — and, error findings being present under the default blocking posture (FR-32's security axiom), the run EXITS 1 (zero degradations); clean fixture (env-var config, parameterized query, `JSON.parse`) yields zero findings, exit 0, and a warm run cache-HITS byte-identically (exactly 4 findings hits — one per registered analyzer); axiom 5 `off` skips the analyzer and declares it in `axiomsOff`; `advisory` persists the `eval` ERROR finding with a passing gate, exit 0 (the bypass durably declared) | `tests/integration/security-rules.e2e.test.ts` |
| 1.13 Axiom #6 conformance rule set (structural tier) | `guardrails review` over the violation fixture (PascalCase basename in a kebab-case directory + a plain source file outside `src/` + a default-bound corpus module the diff modifies, in a named-binding repo) matches `expected-findings.json` byte-for-byte, twice (all three ruleIds, axiom "6", `source: "ast"`, ALL warnings, messages citing measured counts) — and, conformance being advisory by design, the run EXITS 0 with the findings persisted (asserted explicitly, zero degradations); the fixture repos run `guardrails init` so the corpus is REAL (derived by the 1.8 producer from the fixture's own committed 12-module tree, never a hand-written seed) and the diff is overlaid only afterwards; clean fixture yields zero findings, exit 0, and a warm run cache-HITS byte-identically (exactly 4 findings hits — one per enabled analyzer); the manifest records `corpusSeedHash` (the corpus the findings were measured against, distinct from `corpusHash`); the no-corpus fixture (same repo, `init` skipped, seed provably absent) is INCONCLUSIVE — zero findings, the `no_corpus` degradation declared in the persisted artifact with subject `corpus-seed`, PRINTED as a `guardrails review: inconclusive: …` stderr line (an inconclusive run is never byte-identical to a clean one), and exit 0 with no `degraded:` line, proving it does not drive exit 2; axiom 6 `off` skips the analyzer and declares it in `axiomsOff`; `advisory` persists all three findings with a passing gate and the `perAxiom` bypass row, exit 0 | `tests/integration/conformance-rules.e2e.test.ts` |
| 1.14 SPIKE-5 Windows git-worktree lifecycle | None — gate spike plus a library primitive (`withWorktree`/`reclaimWorktrees`); no CLI surface until Story 1.15 wires branch/PR scopes. Covered instead by `packages/core/src/git/worktree.test.ts` (PLATFORM-NEUTRAL, so CI's Linux guards it too: happy path, callback throw + async rejection, a removal that fails WHILE the callback throws with the degradation attached to the propagating error, invalid ref, option-shaped ref rejected, case-collision refs on both case-sensitive and case-insensitive filesystems, ref-derived directory naming, genuinely parallel lifecycles, base-dir degrade, both-bases-unusable, residue with no registration, a prefixed FILE and a stale probe file, registration with no directory, an unreadable registry never reported as removed, attempt clamping, `git worktree lock` honoured, foreign-worktree safety, another REPOSITORY's worktrees invisible in a shared base, a `.git` pointing elsewhere declared not deleted, degrade-then-sweep-both-bases, and the manifest-Degradation round trip) plus `packages/core/src/git/git.test.ts` (a git that WAITS is killed by the timeout). Gated by the committed harness `scripts/spike-5-worktree-lifecycle.mjs` (run manually: `pnpm -r build && node scripts/spike-5-worktree-lifecycle.mjs` — 100 cycles + injected-failure suite + a base-length measurement; a skipped scenario or a leaked temp dir fails the gate; a run below 100 cycles is labelled SMOKE and cannot emit PASS), written up in `docs/spikes/SPIKE-5-windows-worktree-lifecycle.md` with the raw run output committed at `docs/spikes/SPIKE-5-run-output.txt` | n/a |
| 1.15 Review scopes — branch, PR, and project | `guardrails review --branch feature` reviews the branch through a REAL `git worktree` (merge-base diff, not the base tip) and writes into the INVOKING repo's `reviews/branch-feature/` with the exact ref + base + `baseGuessed` on the manifest, the guessed base declared on stderr, no tracked file in the invoking repo touched and no worktree left registered; `--project` reviews every tracked file into `reviews/project/`; `_agentic-guardrails/` is excluded from the change set of ALL FOUR scopes (committed artifacts present on every branch); an absent `--pr` ref exits 2 with the exact `git fetch` command and writes no artifact directory (no network); a hostile ref (`--branch=--upload-pack=…`) and two scope flags (`--pr 1 --project`) both exit 2; a non-TTY run leaves the artifact untracked and unstaged with HEAD unchanged (no ambient commit); bare `guardrails review` still files under `uncommitted` with no `manifest.scope` block (pre-1.15 artifact bytes unchanged); `GUARDRAILS_NO_GH=1` (set for the whole suite) keeps the optional `gh` lookup out of the loop — asserted with a deliberately failing `gh` first on `PATH`, the PR review still completing with the reason declared | `tests/integration/review-scopes.e2e.test.ts` (plus, in-process: `packages/core/src/pipeline/scope.test.ts` — slugging, lossy-hash collision, hostile/absent refs, per-scope self-exclusion; `packages/core/src/pipeline/pipeline.test.ts` — the two-worktree-paths byte-identity + runId test, scope-keyed cache, gh metadata and its four failure modes through an injected runner, worktree removal on a pipeline throw; `packages/core/src/git/git.test.ts` — merge-base diffing, default-base order, `commitPath` leaving the rest of a dirty tree byte-identical and nothing staged on failure; `packages/cli/src/disposition.test.ts` — commit/drop/EOF/`y`+`yes`/Ctrl-C, the no-op re-run, a REAL detached HEAD degrading to a temp copy, and a commit through a repo that HAS a rejecting `pre-commit` hook). Review-fix coverage (adversarial pass): cross-process worktree liveness — a sibling process's marked worktree survives reclamation while an unidentifiable registered one is DECLARED `in-use` (`worktree.test.ts`); the two-worktree byte-identity case now runs the REAL analyzers and asserts the warm run's cache HITS (a leaked absolute path in a cache key or a finding location fails it); an in-place ref scope reads the DIRTY tree (a finding only that path can produce) and declares the divergence; an empty ref diff is declared; an unreadable file at a ref scope is a degradation, not a silent deletion; `withWorktree`'s failure path carries its own degradations; the `gh` outcome participates in the runId |
| 1.16 Scores, trends, and dispositions | `guardrails review` records raw per-axiom severity counts + measured change size on the artifact and prints a version-stamped OD-1 score (`score: N.N/100 · od-1-v1`), 100.0 on a clean run, OMITTED with a declared reason for `--project` (never a fabricated denominator); one trend record per run appends to the COMMITTED `history/trends.jsonl` carrying `recordId` + `runId` + `commitSha` + `scopeKind`, and an identical re-run appends NOTHING (content-addressed id, both writer and aggregator halves); `history/*.jsonl` is provably NOT swallowed by the seeded ignore (asserted with `git check-ignore` — `reviews/` ignored, `history/` committable) so the M1 gate has a data source; `init` seeds both stores empty plus the `merge=union` attribute; a TORN final record (killed writer) is repaired, declared on stderr, and the store stays usable with every surviving record schema-valid; a first run is a declared COLD START (`trend: no comparable previous run`) and not a degradation; the second run prints a per-axiom `Δ -NE` delta that appears NOWHERE in the artifact, and identical inputs still produce byte-identical artifacts with history present (`normalizeCacheTruth`); DR-1 dispositions record NOTHING on a non-TTY by default (no file created) and record every finding as `deferred` under `dispositionPolicy: deferred` — with the policy visible as an FR-31 deviation line, keyed `{runId, findingId}`, and written even though the artifact was DROPPED (the two dispositions are independent); `guardrails trends` renders a self-contained `.cache/trends.html` (no `<link`, no external URL, formula-version stamped), an honest empty state with no history, and DECLARES an invalid history line rather than silently omitting it; `artifactRetention: 2` prunes `reviews/<scope>/` to 2 while committed history keeps its record | `tests/integration/trends-and-dispositions.e2e.test.ts` (plus, in-process: `packages/core/src/git/numstat-ancestry.test.ts` — the `--numstat -z` parser for renames/binary/malformed counts, real-repo range and working-tree measurement, and `--is-ancestor`'s exit 1 read as a negative ANSWER rather than an error; `packages/core/src/persistence/history.test.ts` — missing file as a first run, schema/JSON/torn-tail skips declared by line number, `recordId` dedupe, CRLF checkouts, idempotent append, torn-tail repair-before-append, LF-only writes; `packages/core/src/report/score.test.ts` — EXACT expected score values (never approximate), the 0.1-KLOC floor for tiny and deletion-only diffs, the 0 floor for overwhelming findings, pinned rounding, and `--project` un-scorability; `packages/core/src/report/trends.test.ts` — cold start, nearest-ancestor selection, scope-kind filtering, never comparing a re-run against itself, missing-sha and non-ancestor skips, the `recordId` tiebreak with its declaration, a single invalid line vs a wholly untrustworthy store, a delta for an axiom that STOPPED producing findings, and an unreadable store declared rather than treated as empty; `packages/cli/src/finding-disposition.test.ts` — the non-interactive short-circuit that never touches readline, both policies, the enum mapping, EOF mid-loop, the SIGINT registration, idempotent re-answers vs a KEPT correction, and a write failure that is loud but never fatal; `packages/cli/src/trends-command.test.ts` — script-context escaping (a `</script>` payload in a record cannot split the script element), the JS line terminators legal raw inside JSON, self-containment, the empty state, the derived score, and `null` for a project-scope record; `packages/core/src/persistence/artifact-writer.test.ts` — retention pruning with the mtime+filename tiebreak, committed history untouched, opt-in behaviour, and an unreadable directory never failing a completed review). Review-fix coverage (adversarial pass): a `--branch` run reviewed from ANOTHER head compares against that branch's own previous record (the delta used to require the branch to be merged first); a deletion-only diff scores finitely end to end; `--project` persists NO `changedLines`/`changedKlocMilli` at all; an UNBORN repository records no trend record, says so, and exercises the `score-change-size` measurement-FAILURE degradation (declared, exit-neutral, denominator floored); retention at 101 under the DEFAULT limit keeps exactly 100 and leaves non-`.json` and in-flight `.tmp` entries alone, `artifactRetention` of `0`/`-1`/`1.5` exits 2, and the surviving set is asserted to CONTAIN the artifact this run wrote (not just to have the right length); the "delta stays out of the artifact" test pins the artifact's and scores block's KEY SETS rather than the absence of the word "delta"; in-process — `parseNumstatZ` per-path counts with the engine's own output excluded from the sum plus paths containing spaces/tabs/quotes/non-ASCII; `appendJsonl` refusing a schema-invalid record, KEEPING a valid last record that merely lost its newline, deduping within a batch, and writing every byte of a 500-record payload; whitespace-only / non-object / wrong-record-type history lines; `skipped` counting declared skips rather than deduped duplicates; `appendDispositions` recording a REVERTED answer while still skipping an unchanged one and deduping a shared `findingId` in one batch; a forged `recordId` losing the ancestry tiebreak; the scan cap silent on a two-record store and declared (with the true nearest ancestor surviving) past 200; exact-half score rounding and a zero denominator never dividing; a `__proto__` answer recording nothing; the disposition prompt sanitizing a hostile path and ruleId; the SIGINT handler BODY closing the interface; `openerFor` never naming a command-line re-parser on any platform; `guardrails trends` exit-2 paths (not a repo, unreadable store), a failing `--open` printing the path at exit 0, and a hostile axiom key sanitized out of a history declaration; `pruneScopeDir` still pruning when one entry vanishes between `readdir` and `stat` |
| 1.17 SPIKE-4 noise metric and labeled fixtures (gate) | None — measurement spike, no UI flow; deliverable is `docs/spikes/SPIKE-4-noise-metric.md` (denominator = error/warning findings, DR-1 dispositions live / fixture placement in CI, disposition scope ruled CARRIED) plus the committed baseline `tests/__fixtures__/noise-baseline.json` and the CI counter-metric `tests/integration/noise-metric.e2e.test.ts` (sweeps all five labeled fixture sets through the built CLI, prints the per-analyzer FP/denominator/rate table every run, fails at ≥30% overall or any analyzer above its committed baseline or missing a baseline entry; gate logic pure and hazard-tested in `tests/integration/noise-rate.test.ts` — exactly-30% fails, zero denominator passes declared, info excluded, integer/rational arithmetic only) | n/a |
